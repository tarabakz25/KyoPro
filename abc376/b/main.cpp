#include <bits/stdc++.h>
using namespace std;
using vec = vector<vector<int>>;

#define rep(i, n) for(int i = 0; i < n; i++)
#define rep2(i, a, n) for(int i = a; i < n; i++)

int main()
{
    int n, q;
    cin >> n >> q;
    
    int ans = 0;
    int lnow = 1, rnow = 2;

    rep(i, q){
        char h;
        int t;
        cin >> h >> t;

        if(h == 'L'){
            if()
        }
    }

    cout << ans << endl;
}